/** Image tools registry. */

export interface ImageToolDef {
  slug: string;
  name: string;
  icon: string;
  description: string;
  lead: string;
  widget: 'compress' | 'convert' | 'resize' | 'base64' | 'heic' | 'rotate' | 'circle' | 'split' | 'metadata' | 'annotate' | 'flip' | 'crop' | 'resizekb' | 'watermark' | 'favicon' | 'svgpng' | 'b64img' | 'combine' | 'dpi' | 'pngchunks';
  how: string;
  note?: string;
  faqs: { q: string; a: string }[];
  keywords: string[];
}

export const IMAGE_TOOLS: ImageToolDef[] = [
  {
    slug: 'heic-to-jpg',
    name: 'HEIC to JPG Converter',
    icon: '📲',
    description:
      'Convert iPhone HEIC/HEIF photos to JPG or PNG in your browser — libheif compiled to WebAssembly, no upload, works offline after loading.',
    lead: 'iPhone photo won\'t open? Convert HEIC to universally supported JPG (or PNG) right here — decoded on your device, never uploaded.',
    widget: 'heic',
    how: 'iPhones save photos in HEIC (High Efficiency Image Container, using HEVC compression) — roughly half the size of an equivalent JPEG, but almost nothing outside the Apple ecosystem opens it, and no browser renders it natively. This tool ships libheif — the reference open-source HEIF decoder — compiled to WebAssembly: the decoder downloads to your browser on first use (~1 MB), decodes the photo locally, and re-encodes it as JPG or PNG. The photo itself never travels anywhere.',
    note: 'The upstream fix is worth knowing: iOS Settings → Camera → Formats → "Most Compatible" makes the iPhone shoot JPEG directly, and sharing via Mail or AirDrop often converts automatically. This tool is for the files that already exist as HEIC — email attachments, cloud downloads, WhatsApp "document" sends — and for people who keep HEIC\'s space savings but occasionally need a JPG.',
    faqs: [
      { q: 'Why won\'t my iPhone photo open on Windows or Android?', a: 'Since iOS 11 (2017), iPhones default to HEIC — a container using HEVC compression that stores photos at roughly half JPEG size. Windows needs paid codec extensions, many Android apps and older software can\'t read it, and no web browser displays it natively. Converting to JPG makes it open everywhere.' },
      { q: 'Does converting HEIC to JPG lose quality?', a: 'Marginally — both formats are lossy, so re-encoding costs a little, controlled by the quality slider (85+ is visually transparent). Choose PNG for a lossless re-encode of the decoded image at the cost of a much larger file.' },
      { q: 'Is my photo uploaded to convert it?', a: 'No — the libheif decoder runs as WebAssembly inside your browser. The ~1 MB decoder downloads once on first use; your photo never leaves your device, and conversion works offline afterwards.' },
      { q: 'Can I stop my iPhone creating HEIC files?', a: 'Yes: Settings → Camera → Formats → Most Compatible switches capture to JPEG. The trade-off is roughly double the storage per photo — which is why Apple defaults to HEIC.' },
      { q: 'What about Live Photos and bursts?', a: 'The still frame converts normally — that\'s what a HEIC file presents as its primary image. The motion part of a Live Photo is a separate video file that stays on your phone.' },
    ],
    keywords: ['heic to jpg', 'convert heic to jpeg', 'open heic file', 'heic converter online', 'iphone photo to jpg', 'heic to png'],
  },
  {
    slug: 'image-compressor',
    name: 'Image Compressor',
    icon: '🗜️',
    description:
      'Compress JPEG, PNG and WebP images with a live quality slider and before/after size comparison — processed on your device, never uploaded.',
    lead: 'Shrink image file size with a quality slider and instant before/after comparison — your photo never leaves the browser.',
    widget: 'compress',
    how: 'The image is decoded and re-encoded by the browser\'s own codec at your chosen quality level. Lossy formats (JPEG, WebP) discard detail the eye rarely misses — most photos survive quality 70–85 with no visible change while dropping to a fraction of the size. The size readout updates live so you can find the point where quality loss becomes visible, and re-encoding also strips metadata as a side effect.',
    note: 'The counterintuitive tip: for photographs, converting PNG to JPEG or WebP saves far more than any PNG optimization — PNG is lossless by design and stores photographic noise expensively. Keep PNG for screenshots, diagrams and anything with text or sharp edges, where JPEG artifacts show first.',
    faqs: [
      { q: 'What quality setting should I use?', a: 'For photos, 75–85 is the sweet spot — usually 5–10× smaller than the original with no visible difference at normal viewing size. Go lower for thumbnails, higher (90+) only for print or editing masters.' },
      { q: 'Why is my PNG barely shrinking?', a: 'PNG is lossless — the quality slider doesn\'t apply, and photographic content compresses poorly in it. Convert photos to JPEG or WebP for major savings; keep PNG for screenshots and graphics with text or transparency.' },
      { q: 'Does compression remove metadata too?', a: 'Yes — re-encoding through the canvas produces a file with pixels only, so EXIF, GPS and other metadata are dropped as a side effect. For metadata specifically, the image metadata remover shows what was present first.' },
      { q: 'Is WebP better than JPEG?', a: 'For the same visual quality, WebP files are typically 25–35% smaller, and WebP supports transparency. Every modern browser displays it; the residual reason to pick JPEG is compatibility with old software and some upload forms.' },
      { q: 'Is the image uploaded for processing?', a: 'No — decoding and re-encoding use the browser\'s built-in codecs on your device. Nothing is transmitted; the tool works offline.' },
    ],
    keywords: ['compress image online', 'image compressor', 'reduce image file size', 'compress jpeg', 'compress png'],
  },
  {
    slug: 'image-converter',
    name: 'Image Format Converter',
    icon: '🔁',
    description:
      'Convert images between PNG, JPEG and WebP in your browser — with quality control for lossy targets and transparency handling explained. No upload.',
    lead: 'PNG ↔ JPEG ↔ WebP in one click, locally — with the transparency and quality trade-offs made visible instead of silently mangled.',
    widget: 'convert',
    how: 'The browser decodes the source and re-encodes it in the target format with its native codec. The conversions differ in what they can carry: PNG is lossless and keeps transparency; JPEG is lossy and has no alpha channel — transparent regions get flattened onto a background; WebP does both lossy compression and transparency. The tool warns when a conversion drops something the source had.',
    note: 'The classic mistake this tool prevents: converting a transparent logo to JPEG and getting a white (or black) box behind it. If you need transparency, the answer is PNG or WebP — no JPEG setting fixes it, because the format simply has no alpha channel.',
    faqs: [
      { q: 'Which format should I convert to?', a: 'Photos → JPEG (universal) or WebP (smaller); screenshots, diagrams, text → PNG; anything needing transparency → PNG or WebP. Converting a photo to PNG makes it dramatically larger for zero visible gain.' },
      { q: 'What happens to transparency when converting to JPEG?', a: 'JPEG has no alpha channel, so transparent pixels must be flattened onto a solid background (white by default here). If the result looks wrong, the source needed WebP or PNG instead.' },
      { q: 'Does converting JPEG to PNG improve quality?', a: 'No — JPEG\'s compression loss already happened and is baked into the pixels. PNG conversion just stores those same pixels losslessly in a much bigger file. Converting the other way (PNG → JPEG) does discard detail; do it once, from the original.' },
      { q: 'Can I convert HEIC photos from an iPhone?', a: 'Only if your browser can decode HEIC, which most currently can\'t (Safari can). If the picker accepts the file and a preview appears, the conversion works; otherwise export as JPEG from the Photos app first.' },
      { q: 'Are my images uploaded?', a: 'No — decoding and re-encoding happen with the browser\'s built-in codecs, on your device, offline-capable.' },
    ],
    keywords: ['convert png to jpg', 'image converter online', 'webp to png', 'jpg to webp', 'png to webp converter'],
  },
  {
    slug: 'image-resizer',
    name: 'Image Resizer',
    icon: '📏',
    description:
      'Resize images to exact pixel dimensions with aspect-ratio lock and high-quality downscaling — in your browser, no upload, no watermark.',
    lead: 'Resize to exact pixels — aspect ratio locked by default so nothing gets squashed — and download, all without the image leaving your device.',
    widget: 'resize',
    how: 'The image is redrawn at the target dimensions on a canvas with high-quality resampling enabled, then re-encoded in your chosen format. The aspect-ratio lock recalculates the other dimension as you type, because independent width/height edits are how images get visibly stretched. Downscaling discards pixels (fine); upscaling invents them (soft) — the tool tells you which you\'re doing.',
    note: 'Resolution advice that saves storage: screens are the destination for most images, and a 4000-pixel-wide photo shown in an 800-pixel column wastes 25× the pixels. Resize to roughly the largest display size you need (retina = 2× the CSS size), then compress — the combination routinely cuts files by 90%+.',
    faqs: [
      { q: 'How do I resize without distorting the image?', a: 'Keep the aspect-ratio lock on: set one dimension, and the other follows automatically. Distortion only happens when width and height are changed independently to a different ratio than the original.' },
      { q: 'Can I make an image bigger?', a: 'You can set larger dimensions, but upscaling cannot add detail that was never captured — results look progressively softer. It\'s acceptable for modest bumps (up to ~1.5×); for serious enlargement you\'d want a dedicated AI upscaler.' },
      { q: 'What size should images be for the web?', a: 'Match the largest size they\'ll display: full-width hero images ~1600–2400px wide, content images ~800–1200px, thumbnails 300–400px. Double the CSS pixel size for sharp rendering on high-DPI ("retina") screens.' },
      { q: 'Does resizing reduce file size?', a: 'Dramatically — file size scales roughly with pixel count, so halving both dimensions cuts pixels (and typically bytes) by ~75%. Resizing down is the single most effective image "compression" step.' },
      { q: 'Is the image uploaded to resize it?', a: 'No — the canvas API resamples on your device. Nothing is transmitted, and there\'s no watermark or size cap beyond your device\'s memory.' },
    ],
    keywords: ['resize image online', 'image resizer', 'resize photo to pixels', 'change image dimensions', 'resize image without losing quality'],
  },
  {
    slug: 'image-to-base64',
    name: 'Image to Base64 Converter',
    icon: '🖇️',
    description:
      'Convert an image to a Base64 data URL for embedding in CSS, HTML or JSON — generated locally with size shown honestly (+33% overhead).',
    lead: 'Image in, data: URL out — ready to paste into CSS or HTML, with the 33% size overhead shown so you know when not to.',
    widget: 'base64',
    how: 'The file\'s bytes are Base64-encoded into a data: URL (data:image/png;base64,…) that carries the entire image inside a string — usable anywhere a URL is: CSS background-image, <img src>, JSON payloads. The encoding maps every 3 bytes of image data onto 4 text characters drawn from a 64-symbol alphabet (A–Z, a–z, 0–9, + and /), padding the end with = signs, which is where the fixed 4/3 (≈33%) size increase comes from. Encoding happens in your browser; the output size readout includes that overhead so the cost is visible before you embed.',
    note: 'When to embed and when not to: data URLs shine for small assets (icons, tiny logos under a few KB) where saving an HTTP request matters more than bytes. For anything larger they backfire — 33% bigger, excluded from image caching, and they bloat the document that contains them. If you\'re embedding a 500 KB photo, link it instead.',
    faqs: [
      { q: 'What is a Base64 data URL?', a: 'A URL that contains the resource itself: data:image/png;base64, followed by the image bytes re-spelled as text. Browsers render it like any image URL — no separate file or request needed.' },
      { q: 'Why is the Base64 output bigger than my image?', a: 'Base64 spends 4 text characters per 3 bytes — a fixed 33% overhead. That\'s the price of representing binary as text; it\'s why embedding is best reserved for small assets.' },
      { q: 'Where would I use this?', a: 'Inlining small icons in CSS to cut HTTP requests, embedding images in self-contained HTML files or emails, and carrying images inside JSON APIs. Also handy for testing image handling without hosting a file.' },
      { q: 'Is there a size limit for data URLs?', a: 'Browsers handle multi-megabyte data URLs, but practical limits arrive sooner: some email clients and CSS processors choke on very long strings, and page weight suffers. Keep embedded images small — icons, not photos.' },
      { q: 'How much bigger will the Base64 string be than my file?', a: 'About 33% larger: for every 3 bytes of image, Base64 writes 4 characters. So a 9 KB icon becomes roughly 12 KB of text. That overhead is fixed by the encoding and is the main reason to reserve embedding for small assets.' },
      { q: 'Does encoding to Base64 lose any image quality?', a: 'No — Base64 is a lossless, reversible text representation of the exact same bytes, so decoding it returns the original file unchanged. It only affects size, not the pixels; any quality difference would come from a separate compression step, not the encoding.' },
      { q: 'Is my image uploaded to convert it?', a: 'No — the encoding is a local byte transformation in your browser. The string appears in the output box and nowhere else.' },
    ],
    keywords: ['image to base64', 'base64 image encoder', 'data url generator', 'convert image to data uri', 'png to base64'],
  },
  {
    slug: 'rotate-image',
    name: 'Rotate & Flip Image',
    icon: '🔄',
    description:
      'Rotate an image by 90°, 180° or 270° and flip it horizontally or vertically — then download. Runs entirely in your browser, nothing uploaded.',
    lead: 'Straighten a sideways photo or mirror an image: rotate in quarter turns, flip horizontally or vertically, and download the result — all on your device.',
    widget: 'rotate',
    how: 'The image is redrawn onto a canvas with a transform applied: the canvas is rotated about its centre by the angle you choose, and scaled by −1 on either axis to mirror it. A quarter turn (90° or 270°) swaps the output width and height, so a 4000×3000 photo becomes 3000×4000; a 180° turn keeps the dimensions but reverses both axes. Because these are exact right-angle turns, every source pixel lands squarely on a destination pixel with no interpolation, so the geometry is lossless. The pixels are then re-encoded, so you can also pick the output format and quality. Everything happens in your browser — the photo is never uploaded.',
    note: 'Rotating by exact quarter turns is lossless in terms of geometry — no pixels are interpolated, since every pixel maps exactly onto another. The re-encoding step is what costs a little quality on JPEG, so choose PNG (or a high quality setting) if you plan to edit further. A common use is fixing photos that appear sideways because the EXIF orientation flag was ignored by the app that opened them; re-saving here bakes the correct orientation into the pixels themselves.',
    faqs: [
      { q: 'How do I rotate an image 90 degrees?', a: 'Upload the image, choose 90°, and download. The output width and height swap — a 4000×3000 landscape photo becomes 3000×4000 portrait. Everything is processed in your browser.' },
      { q: 'What is the difference between rotating and flipping?', a: 'Rotating turns the image around its centre, preserving the arrangement of the content. Flipping mirrors it, so text reads backwards and left becomes right. You can combine both here.' },
      { q: 'Why does my photo appear sideways on some devices?', a: 'Cameras often store the photo in its original sensor orientation plus an EXIF "orientation" flag telling viewers how to rotate it. Apps that ignore that flag show it sideways. Rotating and re-saving here bakes the correct orientation into the pixels, so every viewer shows it correctly.' },
      { q: 'Does rotating lose quality?', a: 'The rotation itself is geometrically lossless at 90° increments, since each pixel maps exactly onto another. Any small quality cost comes from re-encoding — pick PNG or a high quality setting to minimise it.' },
      { q: 'What is the difference between 90° and 270°?', a: '90° turns the image a quarter turn clockwise; 270° (the same as −90°) turns it a quarter turn anticlockwise. Both swap the width and height — pick whichever brings a sideways photo upright, and add 180° if it ends up upside-down.' },
      { q: 'Can I rotate by an arbitrary angle like 5°?', a: 'This tool rotates in exact quarter turns (90°, 180°, 270°), which keeps the result lossless and rectangular. A small off-axis angle — to straighten a crooked horizon — would need interpolation and would leave triangular gaps at the corners, so it\'s better handled by a crop-and-straighten tool.' },
      { q: 'Is my photo uploaded?', a: 'No. The image is drawn onto a canvas in your browser and re-encoded locally. It never leaves your device, and the tool works offline once the page has loaded.' },
    ],
    keywords: ['rotate image', 'flip image', 'mirror image online', 'rotate photo 90 degrees', 'image rotator', 'turn image sideways'],
  },
  {
    slug: 'circle-crop-image',
    name: 'Circle Crop Image',
    icon: '⭕',
    description:
      'Crop any image into a circle with a transparent background — perfect for profile pictures and avatars. Exports PNG, processed entirely in your browser.',
    lead: 'Turn any photo into a round avatar: the image is centre-cropped to a square and masked to a circle, exported as a transparent PNG. Nothing is uploaded.',
    widget: 'circle',
    how: 'The tool takes the largest square that fits in the middle of your image, then uses a circular clipping path on a canvas so only the pixels inside the circle are drawn. The corners are left fully transparent, which is why the output is always PNG — JPEG has no alpha channel and would fill those corners with white. The result is a square PNG whose visible content is a perfect circle, ready to drop into a profile field or a design.',
    note: 'Because the crop is taken from the centre, compose accordingly — if your subject sits off to one side, crop the image to roughly square first so the circle lands where you want it. Note that most platforms (social profiles, chat apps) apply their own circular mask to a square photo, so you often do not need a pre-cropped circle for those; this tool matters when you need genuine transparency, such as placing an avatar over a coloured background in a document, slide or web page.',
    faqs: [
      { q: 'How do I crop an image into a circle?', a: 'Upload the image and download — the tool centre-crops it to a square and masks it to a circle automatically. The result is a PNG with transparent corners.' },
      { q: 'Why is the output always PNG?', a: 'The corners outside the circle must be transparent, and only formats with an alpha channel can store transparency. JPEG has none, so saving as JPEG would fill the corners with white instead of leaving them clear.' },
      { q: 'Can I choose which part of the image the circle takes?', a: 'The circle is taken from the centre of the image. If your subject is off-centre, crop or reframe the picture to roughly square first, so that the centre of the file is the part you want inside the circle.' },
      { q: 'Do I need a circular image for a profile picture?', a: 'Usually not — most social platforms and chat apps apply their own circular mask to a square upload. A real circular PNG matters when you need transparency, such as overlaying an avatar on a coloured background in a slide, document or web page.' },
      { q: 'Is my photo uploaded anywhere?', a: 'No. The masking is done on a canvas inside your browser and the PNG is generated locally, so the image never leaves your device.' },
    ],
    keywords: ['circle crop', 'circle crop image', 'round profile picture', 'crop image into circle', 'circular avatar maker', 'round image png'],
  },
  {
    slug: 'split-image',
    name: 'Image Splitter (Grid Maker)',
    icon: '🔲',
    description:
      'Split an image into an even grid of tiles — 3×3 for an Instagram grid, carousel strips, or any rows × columns. Download every tile as PNG. In-browser, no upload.',
    lead: 'Cut any image into a grid: pick rows and columns (or a preset like 3×3 for an Instagram grid), preview the cut lines, and download all the tiles together as a ZIP of numbered PNGs.',
    widget: 'split',
    how: 'The splitter draws your image to an off-screen canvas and copies each grid cell into its own tile PNG, then bundles every tile into a single ZIP you download in one click. Tile size is the image width divided by the number of columns, and the height divided by the number of rows. Everything is done in your browser with the Canvas API and JSZip — the image is never uploaded.',
    note: 'Tiles are numbered by row and column (name_1-1, name_1-2, …) so they stay in order inside the ZIP. For a seamless Instagram 3×3 grid, use a source image with a square or matching aspect ratio; if the dimensions don\'t divide evenly by your grid, each tile uses the floor size and a thin strip at the right/bottom edge may be trimmed.',
    faqs: [
      { q: 'How do I split an image into a grid?', a: 'Choose your image, set the rows and columns (or tap the 3×3 preset), and click download — every tile is saved into a single ZIP of numbered PNGs. It all happens locally in your browser.' },
      { q: 'How do I make an Instagram grid?', a: 'Use the 3×3 preset on a square image. You get nine tiles numbered by position; upload them to Instagram in reverse order (bottom-right first) so they assemble into the full picture on your profile grid.' },
      { q: 'Why might a tile be slightly cropped?', a: 'If the image width or height doesn\'t divide evenly by your columns or rows, the tiles use the rounded-down size, so a few pixels at the far edge may be left out. Use dimensions divisible by your grid for a perfect split.' },
      { q: 'Is my image uploaded?', a: 'No. The splitting is done entirely in your browser with the Canvas API, so the image never leaves your device. It works offline once loaded.' },
    ],
    keywords: ['split image', 'image splitter', 'split image into grid', 'instagram grid maker', 'image grid cutter', 'cut image into pieces', 'photo splitter'],
  },
  {
    slug: 'image-metadata-viewer',
    name: 'Image Metadata Viewer (EXIF)',
    icon: '🔍',
    description:
      'See the hidden EXIF metadata in a photo — camera, settings, date and GPS location — before you share it. Read locally in your browser, never uploaded.',
    lead: 'View the EXIF data embedded in a photo: the camera and lens, exposure settings, the date it was taken, and — importantly — any GPS coordinates that reveal where it was shot.',
    widget: 'metadata',
    how: 'Photos from phones and cameras embed EXIF metadata inside the file: the make and model, aperture, shutter, ISO and focal length, a timestamp, and often the exact GPS location. This viewer reads that data on your device and lays it out, flagging GPS coordinates with a map link so you can see precisely what a picture would reveal. Nothing is uploaded — the image is parsed in your browser.',
    note: 'GPS location is the big privacy concern: a photo posted straight from your phone can pinpoint your home, workplace or child\'s school. Many social platforms strip EXIF on upload, but files shared by email, message or cloud link usually keep it. If this viewer shows a location or other data you don\'t want to share, remove it first with the metadata remover. Screenshots and some edited or exported images carry little or no EXIF.',
    faqs: [
      { q: 'How do I view the EXIF data of a photo?', a: 'Choose the image here and its EXIF metadata — camera, settings, date and any GPS — is displayed, all read locally in your browser. Nothing is uploaded.' },
      { q: 'Does my photo contain my location?', a: 'If GPS was enabled when it was taken, likely yes — and this viewer will flag the coordinates with a map link. Photos straight from a phone often embed precise location, which is why it\'s worth checking before sharing.' },
      { q: 'Why does my image show no metadata?', a: 'It was probably stripped already (many apps and social platforms remove EXIF on upload), or it\'s a screenshot or exported format that doesn\'t store much. That\'s good for privacy — there\'s nothing revealing to share.' },
      { q: 'How do I remove the metadata?', a: 'Use the image metadata remover, which rewrites the photo without the EXIF data. Viewing here shows you what\'s there; the remover takes it out — both entirely in your browser.' },
    ],
    keywords: ['exif viewer', 'image metadata viewer', 'view exif data', 'photo metadata viewer', 'check gps in photo', 'exif data viewer online', 'see photo location'],
  },
  {
    slug: 'annotate-image',
    name: 'Image & Screenshot Annotator',
    icon: '🖊️',
    description:
      'Mark up screenshots and images with arrows, boxes, text and freehand pen, then download a PNG. Free, private annotator that runs in your browser.',
    lead: 'Add arrows, boxes, freehand marks and text to a screenshot or photo, in any colour, then export a PNG — all done on your device, nothing uploaded.',
    widget: 'annotate',
    how: 'Load an image and draw on it with a set of tools — an arrow for pointing things out, a box to highlight a region, a freehand pen, and text labels — choosing the colour and stroke size. Each mark is drawn onto a canvas layered over your image; when you download, everything is flattened into a single PNG. Because the marks are stored as a list of shapes rather than baked in as you draw, Undo can remove the last mark without disturbing the ones before it. It\'s all client-side, so nothing is uploaded.',
    note: 'Great for bug reports, support tickets, how-to guides and design feedback, where you need to point at something and add a note. Because screenshots often contain private or internal information, keeping the editing local means those images never leave your device. The export keeps the original image resolution.',
    faqs: [
      { q: 'How do I add arrows to a screenshot?', a: 'Load your image, pick the Arrow tool and a colour, then click and drag from the start point to where the arrow should point. Add as many as you like, then download the annotated PNG.' },
      { q: 'Can I add text or highlight boxes?', a: 'Yes — use the Text tool to place a label anywhere, and the Box tool to draw a rectangle around the area you want to highlight. There\'s also a freehand pen for circling or scribbling.' },
      { q: 'What format does it export?', a: 'A PNG at the original image\'s resolution, with all your annotations flattened onto it. Just click Download PNG.' },
      { q: 'What colour should I use for annotations?', a: 'A high-contrast colour that doesn\'t appear much in the image — bright red or magenta usually stands out on screenshots of light UI, while a light colour reads better over a dark photo. The colour and stroke width are adjustable per mark, so you can match each annotation to what it sits on.' },
      { q: 'Can I remove a mark I got wrong?', a: 'Yes — Undo peels off the marks one at a time, newest first, so you can step back to before a mistake without clearing everything. The original image underneath is never altered until you export.' },
      { q: 'Are my screenshots uploaded?', a: 'No. All the drawing and exporting happens in your browser with the Canvas API, so internal or sensitive screenshots stay on your device.' },
    ],
    keywords: ['annotate image', 'annotate screenshot', 'add arrows to image', 'markup image online', 'screenshot annotation tool', 'draw on image', 'image markup'],
  },
  {
    slug: 'crop-image',
    name: 'Crop Image',
    icon: '✂️',
    description:
      'Crop an image to a rectangle or a fixed aspect ratio (1:1, 16:9, 4:3…) and download it at full resolution — in your browser, never uploaded.',
    lead: 'Drag a crop box or pick an aspect ratio, then download the exact region at full quality — your image never leaves the browser.',
    widget: 'crop',
    how: 'Cropping keeps a rectangular region of an image and discards the rest. Drag the crop box, or type an exact X, Y, width and height in pixels, and optionally lock it to a common aspect ratio — 1:1 for avatars, 16:9 for video thumbnails, 4:5 for portrait posts. The tool redraws just that region onto a new canvas at the source resolution and exports it as a PNG, so the pixels you keep are not rescaled or re-compressed.',
    note: 'Cropping is lossless for the pixels you keep — unlike resizing, it doesn\'t rescale anything, so text and fine detail stay sharp. Aspect presets matter when a platform expects a shape: 1:1 for most profile pictures, 16:9 for YouTube and most video thumbnails, 4:5 for Instagram portrait. Because it all runs on your device, you can crop screenshots of private documents without them ever touching a server.',
    faqs: [
      { q: 'How do I crop an image to an exact size?', a: 'Type the width and height (and the X, Y position) in pixels into the numeric fields, or drag the crop box and read the size back. The download is that exact pixel region at full resolution.' },
      { q: 'Can I crop to a square or 16:9?', a: 'Yes — pick an aspect-ratio preset (1:1, 4:3, 3:2, 16:9, 9:16 and more) and the crop box locks to that shape; resize it and the ratio is maintained. Choose "Free" for an unconstrained crop.' },
      { q: 'Does cropping reduce image quality?', a: 'No. Cropping removes pixels outside the box but does not rescale the ones inside, and the export is a lossless PNG — so the kept region is pixel-for-pixel identical to the original.' },
      { q: 'What is the difference between cropping and resizing?', a: 'Cropping cuts out a region and keeps its pixels at full size; resizing rescales the whole image to new dimensions. To make a file smaller for an upload form, use the resize or resize-to-KB tools instead.' },
      { q: 'Is my image uploaded to crop it?', a: 'No — the crop is drawn with the browser\'s Canvas API on your device. Nothing is transmitted, so even sensitive screenshots stay private and it works offline.' },
    ],
    keywords: ['crop image', 'crop image online', 'crop picture', 'crop image to square', 'crop to aspect ratio', 'crop photo', 'image cropper'],
  },
  {
    slug: 'flip-image',
    name: 'Flip Image (Mirror)',
    icon: '🔃',
    description:
      'Flip or mirror an image horizontally or vertically and download it — same size and quality, processed on your device, never uploaded.',
    lead: 'Mirror an image left-to-right or top-to-bottom in one click — same dimensions, same quality, done in your browser.',
    widget: 'flip',
    how: 'Flipping mirrors the pixels across an axis: a horizontal flip swaps left and right (a true mirror image), a vertical flip swaps top and bottom. Under the hood the canvas is scaled by −1 on the chosen axis and shifted back into view, which reflects every pixel to the mirrored position on the same-sized canvas — so nothing is scaled down or cropped, only reflected. Because each pixel maps exactly onto another position, the reflection itself is lossless; you can then export as JPEG, PNG or WebP.',
    note: 'Horizontal flip is the common one: it "un-mirrors" selfies (front cameras mirror the preview, so text and faces come out reversed) and builds symmetric layouts. Vertical flip is rarer, mostly for reflections. Flipping is not rotating — a flip reflects the image, a rotation turns it; for quarter-turns use the rotate-image tool.',
    faqs: [
      { q: 'How do I mirror an image?', a: 'Load the image and tick "Flip horizontally" to mirror it left-to-right (or "Flip vertically" for top-to-bottom), then download. You can apply both axes at once for a 180° reflection.' },
      { q: 'Why does my selfie look mirrored?', a: 'Phone front cameras show a mirrored preview so it feels like a mirror, and some save it that way — text and asymmetric details come out reversed. A horizontal flip corrects it.' },
      { q: 'Is flipping the same as rotating?', a: 'No. Flipping reflects the image across an axis; rotating turns it around its centre. This tool flips; use the rotate-image tool for 90°, 180° or 270° turns.' },
      { q: 'Does flipping change the file size or quality?', a: 'The dimensions stay identical and it\'s a simple reflection, so there is no quality loss beyond the normal re-encode. Choose PNG for a lossless result or JPEG/WebP for a smaller file.' },
      { q: 'When would I use a vertical flip?', a: 'It\'s the less common one — mainly for creating a reflection effect (a mirrored copy below an object, like water), correcting an image that was scanned or captured upside-down, or matching the orientation of a flipped layer in a design. For everyday selfie and text fixes you want the horizontal flip.' },
      { q: 'Can I flip both directions at once?', a: 'Yes — applying a horizontal and a vertical flip together reflects the image across both axes, which is the same as rotating it 180°. Use both toggles when you need that combined result.' },
      { q: 'Does flipping remove or alter EXIF data like location?', a: 'Re-encoding the image through the canvas produces a fresh file that generally drops the original EXIF metadata, including any GPS location and camera tags. That is often a privacy benefit, but it also means an orientation tag is not carried over — the pixels themselves are already reflected, so the result displays correctly without one.' },
      { q: 'Why flip an image for a print or vinyl transfer?', a: 'Heat-transfer vinyl and iron-on designs are applied face-down, so any text or logo must be mirrored horizontally first to read correctly once pressed. A horizontal flip produces that mirror image, which is why the flip tool is a common step before cutting or printing a transfer.' },
      { q: 'Is the image uploaded?', a: 'No — the flip is done with the Canvas API in your browser. Your photo never leaves your device and the tool works offline.' },
    ],
    keywords: ['flip image', 'mirror image', 'flip image online', 'flip photo horizontally', 'mirror photo', 'flip picture', 'reverse image'],
  },
  {
    slug: 'resize-image-to-kb',
    name: 'Resize Image to KB (Target File Size)',
    icon: '🎯',
    description:
      'Compress an image to an exact target file size in KB — under 50 KB, 100 KB or 200 KB — for upload forms, in your browser, never uploaded.',
    lead: 'Need a photo under 50 KB or 100 KB for a form? Enter a target size and the tool compresses to fit — locally, no upload.',
    widget: 'resizekb',
    how: 'Many upload forms — passport and visa portals, exam and job applications — cap the file size. The tool hits your target by binary-searching the JPEG or WebP quality: it re-encodes the image at different quality levels to find the highest one that still comes in at or under your target KB, and if even the lowest quality is too big, it progressively reduces the pixel dimensions until it fits. You get the best quality that satisfies the limit.',
    note: 'A target that\'s too tight for a large photo forces heavy compression or downscaling — if the result looks soft, raise the target a little or crop the image first so there are fewer pixels to fit. JPEG suits photos; WebP is smaller for the same quality but a few older upload forms reject it. The compression happens entirely on your device, so an ID photo for a government portal never passes through anyone else\'s server.',
    faqs: [
      { q: 'How do I make an image under 50 KB?', a: 'Load the photo, set the target to 50 KB and choose JPEG, and the tool searches for the quality that lands at or under 50 KB, downscaling the dimensions if needed. It reports the final size and settings.' },
      { q: 'How is the file size reduced without picking a quality myself?', a: 'It binary-searches the encoder quality for you — encoding at several levels to find the highest quality that still meets your KB target — then downsizes the pixels only if compression alone can\'t reach it.' },
      { q: 'Why can\'t it hit a very small target?', a: 'A large, detailed photo has a floor below which it can\'t shrink without heavy downscaling. If the tool reports it can\'t reach your target, raise the target, crop the image, or accept the smallest achievable size it shows.' },
      { q: 'Should I choose JPEG or WebP?', a: 'JPEG is universally accepted by upload forms and best for photos. WebP produces a smaller file at the same quality, but a few older portals reject it — use JPEG if the form is strict.' },
      { q: 'Is my photo uploaded to resize it?', a: 'No — the encoding runs on your device with the browser\'s image codecs. Your image, often an ID or passport photo, never leaves your browser.' },
    ],
    keywords: ['resize image to kb', 'reduce image size to 50kb', 'compress image to 100kb', 'image size reducer for form', 'compress photo to target size', 'reduce image to 20kb', 'passport photo size reducer'],
  },
  {
    slug: 'watermark-image',
    name: 'Add Watermark to Image',
    icon: '©️',
    description:
      'Add a text watermark (name, ©, DRAFT, website) to an image — with size, colour, opacity, position and diagonal tiling — in your browser, never uploaded.',
    lead: 'Stamp your name, ©, or "CONFIDENTIAL" across an image — control size, opacity and position, all locally.',
    widget: 'watermark',
    how: 'A watermark overlays text on an image so copies can be traced or marked. Type your text, set the font size as a percentage of the image width so it scales to any photo, pick a colour and opacity, and choose a position — a single corner or centre, or a tiled diagonal repeat that covers the whole image and is harder to crop out. Sizing by percentage rather than fixed pixels is what keeps the mark looking the same on a small thumbnail and a full-resolution photo; the opacity is applied as the canvas global alpha, so 50% lets half the underlying image show through the text. The tool draws the text onto a canvas over the image with a live preview, then lets you download the result.',
    note: 'A tiled, semi-transparent watermark across the middle is far harder to remove than a single small corner mark. For proofs and drafts, "DRAFT" or "CONFIDENTIAL" at low opacity reads clearly without hiding the content. Because everything runs on your device, you can watermark scans of IDs or contracts before sharing them without uploading the originals anywhere.',
    faqs: [
      { q: 'How do I add a text watermark to a photo?', a: 'Load the image, type your watermark text, adjust the size, colour and opacity, pick a position or turn on tiling, and download. A live preview shows the result before you save it.' },
      { q: 'How do I make a watermark hard to remove?', a: 'Use the tiled diagonal mode at moderate opacity so the text repeats across the whole image. A single small corner mark is trivial to crop or clone out; a full-coverage tile is not.' },
      { q: 'Can I control the transparency?', a: 'Yes — the opacity slider sets how see-through the text is. Around 40–60% is a good balance: visible enough to deter reuse, faint enough not to obscure the content.' },
      { q: 'Does it keep transparency in PNGs?', a: 'Yes — export as PNG to preserve an alpha channel, or choose JPEG/WebP for a smaller file. The watermark is drawn on top of the original pixels at full resolution.' },
      { q: 'What colour and opacity work best?', a: 'It depends on the image: white or light grey at 40–60% opacity reads on most photos, while a dark colour shows better over bright or pale areas. On a busy image, the diagonal tile helps because at least some repetitions fall on contrasting backgrounds. Preview and adjust until the text is legible but not overpowering.' },
      { q: 'Does a watermark actually stop image theft?', a: 'It doesn\'t make copying impossible, but a visible mark deters casual reuse and identifies the source if an image is lifted. A centred, tiled, semi-transparent watermark is far harder to remove cleanly than a small corner logo, which raises the effort enough to discourage most misuse.' },
      { q: 'Is my image uploaded to watermark it?', a: 'No — the text is drawn with the Canvas API in your browser. Sensitive documents and photos stay on your device and the tool works offline.' },
    ],
    keywords: ['add watermark to image', 'watermark photo online', 'text watermark', 'watermark image free', 'copyright watermark', 'tile watermark', 'watermark maker'],
  },
  {
    slug: 'favicon-generator',
    name: 'Favicon Generator',
    icon: '🌐',
    description:
      'Turn one image into a complete favicon set — PNGs, a multi-size favicon.ico, a web manifest and the HTML snippet, zipped — in your browser, never uploaded.',
    lead: 'One image in, a complete favicon set out: favicon.ico, every PNG size, the manifest and the <link> tags — generated locally.',
    widget: 'favicon',
    how: 'A favicon needs several sizes for different devices: 16/32/48 px inside a classic favicon.ico, a 180 px apple-touch-icon for iOS, 192/512 px PNGs for Android and PWAs, plus intermediate sizes. The tool centre-crops your image to a square, renders each size on a canvas, packs 16/32/48 into a real multi-resolution .ico, writes a site.webmanifest and the HTML <link> snippet, and bundles everything into a ZIP — all in the browser.',
    note: 'Start from a square image at least 512×512 for crisp results at every size; simple, high-contrast marks read best at 16 px, where fine detail disappears. Drop favicon.ico at your site root and paste the generated <link> tags into your <head>. The .ico uses the modern PNG-in-ICO format that every current browser reads. Nothing is uploaded — your logo never leaves your machine.',
    faqs: [
      { q: 'How do I create a favicon from an image?', a: 'Upload a square image or logo and the tool renders every required size, builds a multi-resolution favicon.ico, writes the manifest and the HTML <link> tags, and downloads it all as a ZIP. Unzip it into your site and paste in the tags.' },
      { q: 'What sizes does it generate?', a: 'PNGs at 16, 32, 48, 64, 96, 128, 180, 192 and 512 px, plus a favicon.ico containing the 16, 32 and 48 px versions — covering browser tabs, iOS home screens and Android/PWA icons.' },
      { q: 'What is favicon.ico and do I still need it?', a: 'It\'s the multi-size icon file browsers request from your site root by default. It\'s still the most compatible fallback, so the tool builds one (using PNG-in-ICO) alongside the modern PNG links.' },
      { q: 'Where do I put the files and tags?', a: 'Place favicon.ico, the PNGs and site.webmanifest at your site root, then paste the generated <link rel="icon"> and apple-touch-icon tags into the <head> of your HTML. The snippet is included in the ZIP and shown on the page.' },
      { q: 'Is my logo uploaded?', a: 'No — every size is rendered with the Canvas API and zipped with JSZip in your browser. Your logo never leaves your device, which matters for unreleased brand work.' },
    ],
    keywords: ['favicon generator', 'create favicon', 'favicon.ico generator', 'image to favicon', 'favicon from image', 'apple touch icon generator', 'favicon maker'],
  },
  {
    slug: 'svg-to-png',
    name: 'SVG to PNG Converter',
    icon: '🖼️',
    description:
      'Convert an SVG (file or pasted markup) to a raster PNG at any resolution — with a live preview, in your browser, never uploaded.',
    lead: 'Rasterise an SVG to a crisp PNG at the size you choose — paste the markup or load a file, all in the browser.',
    widget: 'svgpng',
    how: 'An SVG is vector artwork — instructions, not pixels — so many apps, upload forms and thumbnails need it flattened to a raster PNG first. The tool loads your SVG into an image, reads its viewBox or width/height to keep the aspect ratio, and draws it onto a canvas at the output size you set, then exports a lossless PNG. You can pick the width, lock the aspect ratio, and choose a transparent or white background.',
    note: 'Because SVG is resolution-independent, you can export at any size without blurring — render at 2× or 3× the display size for retina screens. Choose a white background when the target (a JPEG slot, some chat apps, print) doesn\'t handle transparency; keep transparent for overlays and logos. The conversion runs on your device, so unreleased brand artwork never leaves your machine.',
    faqs: [
      { q: 'How do I convert an SVG to PNG?', a: 'Load the .svg file or paste the <svg> markup, set the output width (height follows the aspect ratio), pick a background, and download the PNG. A live preview shows the result first.' },
      { q: 'What size PNG should I export?', a: 'Match the largest size the PNG will be displayed at, and render at 2× for high-density (retina) screens. SVG is vector, so exporting larger never blurs — it just makes a bigger file.' },
      { q: 'Why is my PNG background black or transparent?', a: 'SVGs are usually transparent. If a target needs a solid background, choose "White"; for overlays and logos keep "Transparent". JPEG has no transparency, which is why flattening to a chosen colour matters.' },
      { q: 'Does it keep the SVG sharp?', a: 'Yes — it renders the vector directly to the chosen pixel size, so edges are crisp at that resolution. Only if you scale the PNG up afterwards will it soften; export larger instead.' },
      { q: 'Is my SVG uploaded?', a: 'No — it\'s rendered with the browser\'s Canvas API on your device. Nothing is transmitted and it works offline.' },
    ],
    keywords: ['svg to png', 'convert svg to png', 'svg to png converter', 'rasterize svg', 'svg to image', 'export svg as png'],
  },
  {
    slug: 'base64-to-image',
    name: 'Base64 to Image',
    icon: '🧩',
    description:
      'Decode a Base64 string or data: URL back into a viewable, downloadable image — the reverse of image-to-Base64, in your browser, never uploaded.',
    lead: 'Paste a Base64 string or data: URL and get the image back — preview it and download it, all locally.',
    widget: 'b64img',
    how: 'Base64 encodes binary image bytes as text so they can be embedded in HTML, CSS, JSON or data: URLs, using 64 safe characters (A–Z, a–z, 0–9, + and /) to represent every 6 bits — which is why encoded text runs about a third larger than the raw file. This tool reverses that: paste the Base64 (with or without the data:image/…;base64, prefix) and it detects the format from the decoded bytes, builds a data URL, previews the image, and lets you download the real file. It\'s the inverse of the image-to-Base64 tool.',
    note: 'If the string starts with data:image/png;base64, it already carries its format; a raw Base64 blob is sniffed from its first bytes (JPEG, PNG, GIF and WebP have distinct signatures). Whitespace and line breaks copied from source code are stripped automatically. Everything is decoded on your device, so a Base64 image pulled from private code or an API response never leaves the browser.',
    faqs: [
      { q: 'How do I convert Base64 back to an image?', a: 'Paste the Base64 text or the full data: URL into the box — the tool decodes it, shows a preview, and gives you a Download button to save the real image file.' },
      { q: 'Do I need the data: URL prefix?', a: 'No. If you include data:image/…;base64, it\'s used directly; if you paste only the raw Base64, the tool detects the image type from the decoded bytes and builds the data URL for you.' },
      { q: 'Why does it say the Base64 is invalid?', a: 'The text isn\'t decodable image data — often a truncated copy, an extra character, or non-image content. Re-copy the whole string; the tool already strips stray whitespace and newlines.' },
      { q: 'What image formats are supported?', a: 'The common web formats — PNG, JPEG, GIF and WebP — are detected from their byte signatures. The download keeps the original format and extension.' },
      { q: 'How does it know the image type without the prefix?', a: 'It reads the first few decoded bytes, called a magic number: PNG starts with 89 50 4E 47, JPEG with FF D8 FF, GIF with the ASCII "GIF8", and WebP with "RIFF"…"WEBP". Those signatures are unambiguous, so the correct type is chosen even from a raw Base64 blob.' },
      { q: 'Why does my decoded image come out larger than expected?', a: 'It won\'t — decoding is exact and reproduces the original file byte-for-byte. It\'s the Base64 text that is inflated (roughly +33%); turning it back into an image removes that overhead, so the downloaded file matches the source image\'s real size.' },
      { q: 'Is anything uploaded?', a: 'No — decoding happens entirely in your browser, so a Base64 image from private source code or an API stays on your device.' },
    ],
    keywords: ['base64 to image', 'base64 decode image', 'data url to image', 'convert base64 to png', 'base64 image decoder', 'base64 to jpg'],
  },
  {
    slug: 'combine-images',
    name: 'Combine Images',
    icon: '🧷',
    description:
      'Merge several images into one — stacked vertically or side by side — with gap and background control, in your browser, never uploaded.',
    lead: 'Stitch multiple images into a single one, vertically or horizontally — set the gap and background, all locally.',
    widget: 'combine',
    how: 'The tool draws your images onto one canvas in the order you add them: vertically stacked (canvas width = the widest image, height = the sum of the heights) or horizontally side by side (height = the tallest, width = the sum of the widths). When you set a gap of G pixels between N images, the tool adds (N−1)×G to that summed dimension and paints the gaps in your chosen background colour. You control the gap, the background colour that fills gaps and any alignment letterboxing, and the output format. Images are drawn at their native size, so nothing is rescaled or softened.',
    note: 'Combining is perfect for stitching a long screenshot from several captures, building a simple before/after pair, or making a photo strip. Because images keep their native size, mismatched widths leave background-coloured margins — crop or resize them to match first if you want flush edges. The whole composition happens on your device; private screenshots are never uploaded.',
    faqs: [
      { q: 'How do I merge images into one?', a: 'Select two or more images, choose vertical (stacked) or horizontal (side by side), set an optional gap and background colour, and download the combined image. A live preview updates as you go.' },
      { q: 'Can I stitch a long screenshot?', a: 'Yes — add the captures in order and choose vertical stacking. Match their widths first (crop or resize) so there are no side margins from the background colour.' },
      { q: 'Why are there coloured margins around some images?', a: 'They have different widths (or heights, for horizontal). The shorter dimension is padded with the background colour to line up. Resize the images to a common width/height for flush edges.' },
      { q: 'Does combining reduce quality?', a: 'No — each image is drawn at its native resolution onto the canvas, so there\'s no rescaling. Export as PNG for a lossless result or JPEG for a smaller file.' },
      { q: 'What size will the combined image be?', a: 'For a vertical stack it\'s as wide as your widest image and as tall as all the heights added together (plus any gaps); for a horizontal row it\'s as tall as your tallest image and as wide as all the widths added (plus gaps). So three 800×600 images stacked with no gap make one 800×1800 image.' },
      { q: 'Does the order I add images matter?', a: 'Yes — images are placed in the order you add them, top-to-bottom for a vertical stack or left-to-right for a horizontal row. Reorder them before exporting to get the sequence you want, which matters for before/after pairs and stitched screenshots.' },
      { q: 'Are my images uploaded?', a: 'No — the merge is done with the Canvas API in your browser. Nothing is sent anywhere and it works offline.' },
    ],
    keywords: ['combine images', 'merge images', 'stitch images together', 'combine photos into one', 'merge images vertically', 'join images side by side', 'image combiner'],
  },
  {
    slug: 'change-image-dpi',
    name: 'Change Image DPI (300 DPI Converter)',
    icon: '🖨️',
    description:
      'Set a PNG or JPEG to 300 DPI (or any value) by editing the density metadata directly — no resampling, so the pixels and quality are untouched. In your browser.',
    lead: 'Set your PNG or JPEG to 300 DPI (or any value) for printing — this edits the DPI metadata only, so the image quality is never changed.',
    widget: 'dpi',
    how: 'DPI (dots per inch) is a metadata tag that tells a printer how large to render an image; it has no effect on how the file looks on screen. This tool reads and rewrites that tag directly in the file — the pHYs chunk in a PNG or the JFIF density fields in a JPEG — without re-encoding the image, so not a single pixel changes. Drop in a PNG or JPEG, see its current DPI, pick a preset (72, 96, 150, 200, 300, 600) or type any value, and download the copy.',
    note: 'This solves a specific, maddening problem: a print shop, print-on-demand service or exam/visa portal demands “300 DPI”, but your image is tagged 72. Crucially, most tools labelled “convert to 300 DPI” actually *resample* the image (adding or dropping pixels, softening it) — that’s the wrong fix when the requirement is really about the metadata tag. This one changes only the tag, losslessly. The catch to understand: DPI plus pixel dimensions together determine the physical print size. 300 DPI on a 300×300-pixel image prints at just 1×1 inch; to print a 4×6&quot; photo at 300 DPI you need 1200×1800 pixels. Setting the tag doesn’t add detail that isn’t there. Everything runs in your browser.',
    faqs: [
      { q: 'How do I change an image to 300 DPI?', a: 'Open your PNG or JPEG, click the 300 preset (or type a value), and download. The tool writes 300 into the file’s density metadata without touching the pixels, so quality is unchanged.' },
      { q: 'Does changing DPI reduce image quality?', a: 'No — this edits only the metadata tag, with no resampling or re-encoding, so the image is byte-for-byte identical except for the DPI value. That’s different from “resize to 300 DPI” tools, which resample and can soften the image.' },
      { q: 'Does higher DPI make my image look better on screen?', a: 'No. On screen an image is shown by its pixels; DPI is ignored. DPI only matters for printing, where it sets how large those pixels are placed on paper.' },
      { q: 'Why does my 300 DPI image still print small (or blurry)?', a: 'Because print size = pixels ÷ DPI. If the image doesn’t have enough pixels, 300 DPI prints small; stretching it larger is what looks blurry. For a 4×6-inch print at 300 DPI you need 1200×1800 pixels — setting the tag can’t add detail that isn’t in the pixels.' },
      { q: 'Is my image uploaded?', a: 'No — the DPI is changed entirely in your browser by editing the file’s bytes, so the image never leaves your device.' },
    ],
    keywords: ['change image dpi', 'convert to 300 dpi', '300 dpi converter', 'set image dpi', 'change dpi of image', 'jpeg dpi changer', 'png dpi', 'increase dpi'],
  },
  {
    slug: 'png-chunk-viewer',
    name: 'PNG Chunk Viewer & CRC Checker',
    icon: '🧩',
    description:
      'Inspect a PNG\'s internal chunks — dimensions, color type, text metadata and DPI — and verify each chunk\'s CRC-32 to catch corruption. In your browser, never uploaded.',
    lead: 'Drop a PNG to see its chunk structure (IHDR, pHYs, tEXt and more), read its hidden text metadata, and check every chunk\'s CRC for corruption.',
    widget: 'pngchunks',
    how: 'A PNG file is a signature followed by a sequence of "chunks", each with a length, a four-letter type, its data, and a CRC-32 checksum. This tool walks that structure and decodes the ones that matter: IHDR (width, height, bit depth and color type), pHYs (physical resolution / DPI), tEXt and iTXt (embedded text metadata like Software, Author or a comment), gAMA, sRGB, tIME, the palette, and the image-data chunks. Critically, it recomputes each chunk\'s CRC-32 from its bytes and compares it to the checksum stored in the file, flagging any mismatch — which is exactly how a PNG decoder detects corruption.',
    note: 'This is useful for debugging a PNG that won\'t open, checking what metadata (and which software) is embedded in an image, confirming a file\'s true dimensions and color type, or spotting a corrupted or tampered chunk via a failing CRC. It reads uncompressed chunk metadata; the compressed pixel data (IDAT) and compressed text (zTXt) aren\'t decompressed, just measured. Everything is parsed in your browser, so the image — and any metadata it carries — never leaves your device.',
    faqs: [
      { q: 'What are PNG chunks?', a: 'A PNG is built from chunks: each has a length, a four-letter type (like IHDR, IDAT, tEXt), its data, and a CRC-32. Chunks with an uppercase first letter are "critical" (needed to display the image); lowercase ones are "ancillary" (optional metadata). This tool lists them all and decodes the common ones.' },
      { q: 'How does the CRC check detect corruption?', a: 'Every PNG chunk stores a CRC-32 computed over its type and data. This tool recomputes that CRC from the bytes and compares it to the stored value. A mismatch means the chunk\'s bytes were altered or corrupted — the same check a PNG decoder uses to reject a broken file.' },
      { q: 'Can I see what metadata is hidden in a PNG?', a: 'Yes — tEXt and iTXt chunks hold text metadata such as the software that created the image, an author, a description or a comment. The tool decodes the uncompressed ones and shows the keyword and value, so you can see what a PNG is carrying.' },
      { q: 'What do bit depth and color type mean?', a: 'They come from the IHDR chunk. Bit depth is bits per channel (often 8); color type says whether the image is grayscale, truecolor (RGB), indexed (palette), or has an alpha channel (RGBA). Together they define how the pixels are stored.' },
      { q: 'Does it change the PNG?', a: 'No — this is a read-only inspector. It parses and verifies the file without modifying it, and to change a PNG\'s DPI specifically, use the dedicated Change Image DPI tool.' },
      { q: 'Is my image uploaded?', a: 'No — the PNG is parsed entirely in your browser and nothing is transmitted, so the image and its metadata stay on your device. It works offline too.' },
    ],
    keywords: ['png chunk viewer', 'png chunk inspector', 'png crc checker', 'png metadata viewer', 'read png chunks', 'png ihdr', 'inspect png file', 'png corruption check'],
  },
];
